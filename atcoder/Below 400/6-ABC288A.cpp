#include <bits/stdc++.h>

using namespace std;

#define int long long

signed main() {
    int n;
    cin >> n;
    while(n--) {
        int a, b;
        cin >> a >> b;
        cout << (a+b) << '\n';
    }
    return 0;
}
